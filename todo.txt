1- refactor filters code and cli code
2- edit filterOne so that it return the hole lines from all the files then you make all the functions
3- add sort when you filter


" end sort function "

.sort((a, b) => {
  const aArr = a.split(",");
  const bArr = b.split(",");
  const yearA = parseInt(aArr[0]);
  const monthA = parseInt(aArr[1]);
  const yearB = parseInt(bArr[0]);
  const monthB = parseInt(bArr[1]);

  if (yearA < yearB && monthA < monthB) {
    return 1;
  } else if (yearA > yearB && monthA > monthB) {
    return -1;
  }

  return 0;
});

" end sort function "




NOOOOOOOOOOOOOTEEEE:

first you should return all lines as an array from all files
then sort the lines and filter them as you want and caclulate the productivity of the filterd lines


